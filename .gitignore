# Environment variables
.env
.env.local
.env.*.local

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
.venv/
ENV/
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
/lib/
/lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Node
node_modules/
npm-debug.log
yarn-error.log

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Claude Code
.claude/

# Local utility scripts + generated summaries (ไม่ใช่ส่วนของโปรเจกต์)
generate_summary_pdf.py
Chat_Summary_*.pdf
System_Capability_Report.docx

# ข้อมูล geodata ดิบ (shapefile/zip) — ก้อนใหญ่ ปลายทางคือ GEE asset ไม่ commit เข้า repo
# เก็บต้นฉบับไว้นอก repo (เช่น Desktop/GreenArea_Data/) · โค้ดใช้ผ่าน LDD_LANDUSE_ASSET
Landuse_Bangkok_2566/
Landuse_Bangkok_2566*.zip
*.shp
*.dbf
*.shx
*.sbn
*.sbx

# รูปเล่มวิทยานิพนธ์ (.docx/.pdf + ไฟล์ BACKUP อัตโนมัติ) — binary ที่ diff ไม่ได้
# เก็บต้นฉบับไว้นอก repo / ใช้ version history ของ Word แทน
รูปเล่ม/

# ผลลัพธ์จาก validate_green_area.py (NFR-08) — regenerate ได้ทุกครั้งที่รัน
# สรุปตัวเลขจริงถูกคัดลงใน REQUIREMENTS.md §5 แล้ว ไม่ต้อง commit ไฟล์ดิบ
green-area-backend/reports/

# รูป .png ที่ scripts/ ในโฟลเดอร์เดียวกันวาดออกมา — regenerate ได้ด้วย
#   green-area-backend/.venv/bin/python "ภาพร่างไดอะแกรมใหม่/scripts/gen_class.py"
# (ต้องมี Pillow + ~/Library/Fonts/THSarabunNew.ttf) · ตัวสคริปต์ track ไว้แล้ว
# ฉบับที่ใช้จริงฝังอยู่ใน .docx ซึ่งเก็บนอก repo ตามนโยบายด้านบน
ภาพร่างไดอะแกรมใหม่/*.png
