# ร่างหัวข้อ "ข้อจำกัดของระบบ" และ "ข้อเสนอแนะและงานในอนาคต"

> สำหรับใส่ในบทที่ 5 (สรุปผล อภิปรายผล และข้อเสนอแนะ)
> ทุกข้อในร่างนี้ตรวจสอบกับโค้ดและ `migrations/` จริงแล้ว — ตัวเลขและชื่อไฟล์อ้างอิงได้
> จัดรูปแบบตามคู่มือสารนิพนธ์ ม.พะเยา 2567 ตอนย้ายเข้าไฟล์ .docx (หัวข้อหนา 16 พอยต์)

---

## 5.x ข้อจำกัดของระบบ

### 5.x.1 ข้อจำกัดด้านข้อมูลนำเข้า

1. **ความถี่และคุณภาพของภาพดาวเทียม** ค่า NDVI และ LST คำนวณจากภาพ Sentinel-2 และ
   Landsat ผ่าน Google Earth Engine ซึ่งช่วงฤดูฝนของประเทศไทยมีเมฆปกคลุมหนาแน่น
   ทำให้บางจังหวัดในบางปีมีภาพที่ใช้ได้น้อย ระบบจึงรายงานคุณภาพข้อมูลกำกับไว้
   (`ndvi_annual.data_quality`) แต่ไม่สามารถชดเชยภาพที่ขาดหายได้

2. **ความคลาดเคลื่อนของการจำแนกพื้นที่สีเขียว** ผลตรวจสอบเทียบ ESA WorldCover
   (NFR-08) ได้ค่าความคลาดเคลื่อนเฉลี่ย (MAE) 5.53 จุดเปอร์เซ็นต์ โดยมี 60 จาก 77
   จังหวัดอยู่ในเกณฑ์เป้าหมาย ±10% หมายความว่ายังมี **17 จังหวัดที่คลาดเคลื่อนเกินเกณฑ์**
   ซึ่งผู้ใช้งานควรตีความผลของจังหวัดเหล่านั้นด้วยความระมัดระวัง

3. **ข้อมูลอ้างอิงบางชุดเป็น epoch เดียว ไม่ใช่รายปี** ชั้นข้อมูลสิ่งปกคลุมดิน
   ESA WorldCover ที่ใช้กำหนดขอบเขต "เขตเมือง" มีเพียงปี ค.ศ. 2021 และข้อมูลประชากร
   WorldPop มีถึงปี ค.ศ. 2020 ค่าตัวชี้วัดระดับเขตเมืองในปีอื่นจึงใช้ปีอ้างอิงเดียวกัน
   (เก็บไว้ในคอลัมน์ `worldcover_year` และ `worldpop_year`) การเปลี่ยนแปลงของขอบเขตเมือง
   ระหว่างปีจึงยังไม่ถูกสะท้อนในผลลัพธ์

4. **ช่วงเวลาของข้อมูลย้อนหลัง** ระบบให้บริการข้อมูลตั้งแต่ปี พ.ศ. 2558 เป็นต้นมา
   ตามช่วงที่ภาพ Sentinel-2 ครอบคลุมประเทศไทยอย่างสม่ำเสมอ จึงยังไม่เพียงพอ
   สำหรับการวิเคราะห์แนวโน้มระยะยาวเกิน 10 ปี

### 5.x.2 ข้อจำกัดด้านโครงสร้างฐานข้อมูล

1. **การแยกตารางระดับจังหวัดและระดับอำเภอออกจากกัน** ระบบใช้ตารางคู่ขนาน 4 คู่
   (`ndvi_annual`/`district_ndvi_annual`, `ndvi_monthly`/`district_ndvi_monthly`,
   `province_lst_annual`/`district_lst_annual`, `province_lst_monthly`/`district_lst_monthly`)
   ซึ่งมีโครงสร้างคอลัมน์เกือบเหมือนกัน ทำให้ทุกฟีเจอร์ใหม่ต้องพัฒนาสองชุด และเกิด
   ความไม่สอดคล้องขึ้นแล้วจริง คือ `district_ndvi_annual` ยังไม่มีคอลัมน์
   `who_status`, `population`, `population_year`, `green_area_m2_per_person` และ
   `validation` ที่ `ndvi_annual` มี

2. **การใช้ชื่อจังหวัดเป็นคีย์หลัก** ตาราง `provinces` ใช้ `name_en` (ข้อความ)
   เป็นคีย์หลัก และตารางข้อเท็จจริงทุกตารางอ้างอิงด้วยข้อความเดียวกัน ทำให้
   การเปลี่ยนชื่อจังหวัดกระทบทั้งฐานข้อมูล ดัชนีมีขนาดใหญ่กว่าที่ควร และการเชื่อมตาราง
   ต้องเปรียบเทียบข้อความทุกครั้ง ขณะที่ตาราง `districts` กลับใช้คีย์ตัวเลข (`id`)
   จึงไม่สอดคล้องกันภายในสคีมาเดียวกัน

3. **การเก็บค่าที่คำนวณได้ซ้ำไว้ในฐานข้อมูล** คอลัมน์ `green_area_pct`,
   `green_area_m2_per_person` และ `who_status` เป็นค่าที่คำนวณจากคอลัมน์อื่นในแถวเดียวกัน
   แต่เขียนโดยโปรแกรมประยุกต์ จึงมีความเสี่ยงที่แต่ละเส้นทางการประมวลผลจะใช้สูตรต่างกัน
   เช่นเดียวกับ `population`/`population_year` ที่ซ้ำกับตาราง `province_population`

4. **การเก็บค่ารายเดือนเป็นก้อนข้อมูล JSON** คอลัมน์ `monthly_data` เก็บค่า 12 เดือน
   ไว้เป็น JSONB หนึ่งค่า ทำให้สืบค้นหรือรวมยอดเฉพาะบางเดือนโดยตรงจากฐานข้อมูลไม่ได้
   ต้องอ่านทั้งก้อนมาประมวลผลที่ชั้นโปรแกรมประยุกต์

5. **การเก็บขอบเขตพื้นที่เป็น JSON แทนชนิดข้อมูลเชิงพื้นที่** คอลัมน์
   `saved_areas.geometry` เก็บเป็น JSONB จึงใช้ความสามารถเชิงพื้นที่ของฐานข้อมูล
   (เช่น การหาพื้นที่ทับซ้อน การค้นหาด้วยดัชนีเชิงพื้นที่) ไม่ได้ ต้องคำนวณที่ฝั่งผู้ใช้

6. **การระบุความเป็นเจ้าของพื้นที่มีสองช่องทาง** ตาราง `saved_areas` มีทั้ง `user_id`
   (ผู้ใช้ที่ลงทะเบียน) และ `owner_token` (โทเคนต่อเครื่องของผู้ใช้เดิมก่อนมีระบบสมาชิก)
   ซึ่งยังคงไว้เพื่อความเข้ากันได้ย้อนหลัง ทำให้ต้องตรวจสอบสิทธิ์สองเส้นทาง

7. **ตารางข้อเท็จจริงไม่มีคอลัมน์เวลาที่ปรับปรุงล่าสุด** ทั้งที่กระบวนการนำเข้าข้อมูล
   เป็นการเขียนทับซ้ำ (upsert) จึงตรวจสอบย้อนหลังไม่ได้ว่าข้อมูลแถวใดถูกคำนวณใหม่เมื่อใด

### 5.x.3 ข้อจำกัดด้านฟังก์ชันการทำงาน

1. **ผู้เยี่ยมชมที่ยังไม่ลงทะเบียนเข้าถึงข้อมูลได้จำกัด** ระบบกำหนดให้ต้องยืนยันตัวตน
   ก่อนเรียกใช้ทุกส่วนของ API ยกเว้นการจัดอันดับพื้นที่สีเขียวระดับประเทศ เพื่อควบคุม
   ปริมาณการเรียกใช้ Google Earth Engine ซึ่งมีโควตาจำกัด

2. **ยังไม่มีส่วนจัดการผู้ใช้งานสำหรับผู้ดูแลระบบ** การกำหนดสิทธิ์ (`profiles.role`)
   ต้องทำผ่านฐานข้อมูลโดยตรง ระบบเปิดให้ผู้ดูแลทำได้เพียงการล้างหน่วยความจำแคช

3. **ยังไม่มีความสามารถตามข้อกำหนดบางข้อ** ได้แก่ FR-18 (สัดส่วนประชากรที่เข้าถึง
   พื้นที่สีเขียวภายในระยะ 300 เมตร), FR-19 และ FR-20 (การวิเคราะห์การเข้าถึงด้วย
   ระยะทางบนโครงข่ายถนนและวิธี 2SFCA), FR-21 และ FR-22 (ปัจจัยความเปราะบางทางสังคม
   และดัชนี Green/Tree Equity Score) ส่วน FR-26 (ปัจจัยความเป็นไปได้ในการปลูกจริง)
   ทำได้เพียงบางส่วน คือใช้เป็นตัวกรองตำแหน่ง แต่ยังไม่ได้เป็นตัวแปรในสูตรจัดลำดับ

4. **เวลาประมวลผลกรณีไม่มีข้อมูลในแคช** การคำนวณค่าจาก Google Earth Engine ใช้เวลา
   ประมาณ 50–60 วินาทีต่อจังหวัด ซึ่งอยู่ในเกณฑ์ NFR-01 แต่ยังถือว่านานสำหรับ
   การใช้งานแบบโต้ตอบ ระบบจึงต้องอาศัยการเตรียมข้อมูลล่วงหน้าเป็นหลัก

---

## 5.y ข้อเสนอแนะและงานในอนาคต

### 5.y.1 การปรับปรุงโครงสร้างฐานข้อมูล

1. **รวมตารางระดับจังหวัดและอำเภอเป็นตารางเดียว** โดยสร้างตารางหน่วยการปกครอง
   (`admin_areas`) ที่มีคอลัมน์ระบุระดับ (จังหวัด/อำเภอ) และคีย์อ้างอิงตัวเอง
   แล้วให้ตารางข้อเท็จจริงอ้างอิงด้วยคีย์ตัวเลขของหน่วยการปกครองแทนชื่อ วิธีนี้
   ลดจำนวนตารางจาก 10 เหลือ 5 ทำให้คำสั่งค้นหาระดับจังหวัดและอำเภอใช้ชุดเดียวกัน
   และตัดปัญหาความไม่สอดคล้องระหว่างตารางคู่ขนานออกไปทั้งหมด

2. **เปลี่ยนคีย์หลักของหน่วยการปกครองเป็นคีย์ตัวแทน** ใช้เลขลำดับหรือรหัสมาตรฐาน
   (เช่น รหัสเขตการปกครอง TIS 1099) เป็นคีย์ แล้วเก็บชื่อภาษาไทยและอังกฤษเป็น
   คุณลักษณะธรรมดา เพื่อให้การเปลี่ยนชื่อไม่กระทบข้อมูลอื่น

3. **แยกค่ารายเดือนเป็นแถวข้อมูล** เปลี่ยน `monthly_data` เป็นตารางที่มีคีย์หลัก
   (หน่วยพื้นที่, ปี, เดือน) ปริมาณข้อมูลที่เพิ่มขึ้นอยู่ในระดับที่จัดการได้
   (ประมาณ 928 อำเภอ × 12 เดือน ≈ 11,000 แถวต่อปี) แลกกับความสามารถในการสืบค้น
   และการกำหนดข้อจำกัดความถูกต้องได้โดยตรง

4. **ย้ายขอบเขตพื้นที่ไปใช้ชนิดข้อมูลเชิงพื้นที่ของ PostGIS** พร้อมสร้างดัชนีเชิงพื้นที่
   เพื่อรองรับการวิเคราะห์การทับซ้อนและการค้นหาตามตำแหน่งในอนาคต

5. **เปลี่ยนค่าที่คำนวณได้ให้เป็นคอลัมน์ที่ฐานข้อมูลคำนวณเอง** (generated column)
   หรือมุมมองที่สร้างไว้ล่วงหน้า (materialized view) เพื่อประกันว่าทุกเส้นทาง
   การประมวลผลใช้สูตรเดียวกัน และเพิ่มคอลัมน์เวลาปรับปรุงล่าสุดให้ทุกตารางข้อเท็จจริง

6. **กำหนดชุดค่าที่อนุญาตให้ชัดเจน** สำหรับคอลัมน์ที่มีค่าจำกัด เช่น ภูมิภาค
   และสถานะเทียบเกณฑ์ WHO โดยใช้ข้อจำกัดตรวจสอบค่าหรือชนิดข้อมูลแจงนับ

### 5.y.2 การต่อยอดเชิงวิเคราะห์

1. **เพิ่มมิติการเข้าถึงพื้นที่สีเขียว** พัฒนาตามข้อกำหนด FR-18 ถึง FR-20 คือคำนวณ
   สัดส่วนประชากรที่เดินถึงพื้นที่สีเขียวสาธารณะภายในระยะ 300 และ 500 เมตร
   โดยใช้ระยะทางบนโครงข่ายถนนแทนระยะทางตรง และประยุกต์วิธี 2SFCA ที่ถ่วงทั้งขนาด
   ของพื้นที่สีเขียวและความหนาแน่นของประชากร

2. **เพิ่มมิติความเป็นธรรมทางสิ่งแวดล้อม** พัฒนาตามข้อกำหนด FR-21 และ FR-22 คือ
   นำสัดส่วนผู้สูงอายุ เด็ก และระดับการรับสัมผัสความร้อน มาถ่วงน้ำหนักในสูตรจัดลำดับ
   ความสำคัญ และคำนวณดัชนีความเป็นธรรมด้านพื้นที่สีเขียวรายอำเภอ เพื่อให้การจัดลำดับ
   ให้น้ำหนักกับชุมชนเปราะบางมากขึ้น

3. **ยกระดับปัจจัยความเป็นไปได้ในการปลูกจริงตาม FR-26** จากปัจจุบันที่ใช้เป็นตัวกรอง
   ให้กลายเป็นตัวแปรที่มีน้ำหนักในสูตรคะแนนความเร่งด่วน โดยรวมข้อจำกัดการใช้ประโยชน์
   ที่ดินและกรรมสิทธิ์ที่ดินเข้ามาพิจารณา

4. **ขยายช่วงข้อมูลย้อนหลังและปรับปรุงความแม่นยำ** โดยผสานภาพจาก Landsat เพื่อขยาย
   ช่วงเวลาให้ยาวขึ้น และปรับปรุงเกณฑ์จำแนกพื้นที่สีเขียวเพื่อลดความคลาดเคลื่อน
   ของ 17 จังหวัดที่ยังเกินเกณฑ์ ±10%

### 5.y.3 การต่อยอดเชิงระบบ

1. **พัฒนาส่วนจัดการผู้ใช้งานสำหรับผู้ดูแลระบบ** ให้กำหนดสิทธิ์และระงับบัญชีได้
   ผ่านหน้าจอ พร้อมบันทึกร่องรอยการเปลี่ยนแปลงสิทธิ์ (audit log)

2. **เปิดเผยข้อมูลบางส่วนแก่ผู้เยี่ยมชมทั่วไป** โดยจัดเตรียมชุดข้อมูลสรุปที่คำนวณไว้
   ล่วงหน้าและจำกัดอัตราการเรียกใช้ เพื่อให้เข้าถึงข้อมูลพื้นฐานได้โดยไม่กระทบโควตา
   ของ Google Earth Engine

3. **เพิ่มการนำเข้าข้อมูลอัตโนมัติตามกำหนดเวลา** เพื่อให้ข้อมูลรายปีถูกคำนวณล่วงหน้า
   ทันทีที่ภาพดาวเทียมของปีนั้นครบถ้วน แทนการรอให้ผู้ใช้เรียกครั้งแรก

4. **พัฒนาเป็นบริการข้อมูลเปิด (Open Data API)** ให้หน่วยงานท้องถิ่นและนักวิจัย
   นำค่าตัวชี้วัดไปใช้ต่อได้ พร้อมเอกสารกำกับและการอ้างอิงแหล่งที่มา
