# Direct dependencies (human-edited source of truth).
# Compile the fully-pinned, hashed lockfile with:
#
#   uv pip compile requirements.in --generate-hashes -o requirements.txt
#
# (or: pip-compile --generate-hashes requirements.in)
#
# requirements.txt is the generated lock — install from THAT, not this file.
fastapi==0.138.0
uvicorn[standard]==0.49.0
supabase==2.31.0
# dependencies.require_user() import `jwt` + PyJWKClient ตรงๆ เพื่อ verify Supabase
# access token ในเครื่อง (ES256/RS256 ต้องมี extra [crypto]) — เดิมติดมาโดยบังเอิญ
# ผ่าน supabase-auth เท่านั้น ถ้าวันหลัง supabase เปลี่ยน lib ภายใน auth จะพังทั้งระบบ
# จึงประกาศเป็น direct dependency ให้ตรงกับที่โค้ดใช้จริง
pyjwt[crypto]==2.13.0
python-dotenv==1.2.2
earthengine-api==1.7.30
httpx==0.28.1
matplotlib==3.10.9
Pillow==12.2.0
requests==2.34.2
slowapi==0.1.9

# Dev/test only
pytest==9.0.3
